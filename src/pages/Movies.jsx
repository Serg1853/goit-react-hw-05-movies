// import { Link } from "react-router-dom";

const Movies = () => {
  // ueseEffect (()=>{
  // HTTP запит
  // },[])

  return (
    <div>
      {/* {movies.map(movie => {
        return (
          <Link key={movie} to={`${movie}`}>
            {movie}
          </Link>
        )
      })} */}
      <h1>Movies page</h1>
    </div>
  );
};
export default Movies;
